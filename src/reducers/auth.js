const initialState = {
  deviceId: "",
  uuid: "",
  username: "",
};

const auth = (state = initialState, {type, payload }) => {
  switch (type) {
    default: {
      return state;
    }
  }
};

export default auth;
