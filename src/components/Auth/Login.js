import React, { Component } from 'react';
import { Text, View, Button, Image, TouchableOpacity, ImageBackground, TouchableHighlight } from 'react-native';
import { Actions } from 'react-native-router-flux';
import styles from './styles';

class Auth extends Component {

  render(){
    return(   
      <View>
        <Text>sang</Text>
      </View> 
    )
  }
}
export default Auth;