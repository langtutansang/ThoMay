import {StyleSheet, Dimensions} from 'react-native';
export default StyleSheet.create({
  main: {
    display: 'flex',
    alignItems: 'center',
    height: "100%",
    width: "100%",
    position:"relative"
  }
})